[pytest]
markers =
    smoke: schnelle Praesenz-Checks (Metriken/Logs/Infra/Freshness)
    e2e: End-to-End ueber Traffic (Traces, korrelierte Logs, Korrelation)
    pipeline: separater Pipeline-Test via telemetrygen (Docker noetig)
addopts = -ra
